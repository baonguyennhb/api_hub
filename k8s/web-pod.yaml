apiVersion: v1
kind: Pod
metadata:
  name: api-hub-server-pod
  labels:
    component: web-server
spec:
  containers:
    - name: api-hub-server
      image: 192.168.5.220:5000/api-hub-server:v2.3
      # volumeMounts:
      #   - name: sqlite # sqlite volume
      #     mountPath: /usr/src/app/Database # mounted at /app/Database in the container
      ports:
        - containerPort: 4000
  # volumes:
  #   - name: sqlite
  #     hostPath: # hostPath volume
  #       path: /var/lib/api-hub