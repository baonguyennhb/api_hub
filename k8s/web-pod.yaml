# apiVersion: v1
# kind: Pod
# metadata:
#   name: api-hub-server-pod
#   labels:
#     component: api-hub-server-pod
# spec:
#   containers:
#     - name: api-hub-server
#       image: 192.168.196.28:5000/api-hub-server:v1.0
#       # volumeMounts:
#       #   - name: sqlite # sqlite volume
#       #     mountPath: /usr/src/app/Database # mounted at /app/Database in the container
#       ports:
#         - containerPort: 4000
#   # volumes:
#   #   - name: sqlite
#   #     hostPath: # hostPath volume
#   #       path: /var/lib/api-hub